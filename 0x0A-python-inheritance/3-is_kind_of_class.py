#!/usr/bin/python3
"""a functions that defines a class and checks an inherutaed class"""

def is_kind_of_class(obj, a_class):
	"""checks if an object in instance or inheritaed instance of a class

	args:
		obj(any):the object to check.
		a_class(type):the class to match the type of the object to.

	return: if the instance or inherited instance of a class return
	True 
		otherwise False
	"""
		
	if isinstance(object.a_class)
		return True
	return False
